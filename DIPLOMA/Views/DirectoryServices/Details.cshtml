@model DIPLOMA.Models.DirectoryServices

@{
    ViewData["Title"] = Model.Service + " - Служба";
}

<h1 class="mb-5">Детально</h1>

<div>
    <h4>Служба <b>@Html.DisplayFor(model => model.Service)</b></h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Service)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Service)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.DirectoryServicesID" class="btn btn-secondary">Изменить</a>
    <a asp-action="Index" class="btn btn-outline-dark">Назад</a>
</div>
