@model DIPLOMA.Models.DirectoryTypeRooms

@{
    ViewData["Title"] = Model.TypeRoom + " - Удаление";
}

<h1 class="mb-5">Удаление</h1>

<h3>Вы действительно хотите удалить данный вид номера?</h3>
<div>
    <h4>Вид номера <b>@Html.DisplayFor(model => model.TypeRoom)</b></h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.TypeRoom)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.TypeRoom)
        </dd>
    </dl>

    <form asp-action="Delete">
        <input type="hidden" asp-for="DirectoryTypeRoomsID" />
        <input type="submit" value="Удалить" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-outline-dark">Назад</a>
    </form>
</div>
