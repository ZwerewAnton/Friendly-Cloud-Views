@model DIPLOMA.Models.DirectoryCategoryRooms

@{
    ViewData["Title"] = Model.CategoryRoom + " - Категория номеров";
}

<h1 class="mb-5">Детально</h1>

<div>
    <h4>Категория номеров <b>@Html.DisplayFor(model => model.CategoryRoom)</b></h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.CategoryRoom)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.CategoryRoom)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.DirectoryCategoryRoomsID" class="btn btn-secondary">Изменить</a>
    <a asp-action="Index" class="btn btn-outline-dark">Назад</a>
</div>
